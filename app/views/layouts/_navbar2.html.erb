<nav class="navbar navbar-expand-lg">
  <div class="container-fluid">

    <a class="navbar-brand">What's New</a>

    <ul class="navbar-nav me-5">
      <li class="nav-item"><a class="nav-link" href="/users">Users</a></li>
      <li class="nav-item"><a class="nav-link" href="/messages">Messages</a></li>
      <li class="nav-item"><a class="nav-link" href="/chats">Chats</a></li>
    </ul>

    <% if current_page? new_user_session_path %>
      <%= link_to "Or Sign Up", new_user_registration_path, class: "btn btn-outline-dark" %>
    <% elsif user_signed_in? %>
      <%= button_to "Logout", destroy_user_session_path, method: :delete, data: { turbo: false }, class: "btn btn-outline-dark" %>
    <% else %>
      <%= link_to "Log In", new_user_session_path, class: "btn btn-outline-dark ms-3" %>
    <% end %>

  </div>
</nav>